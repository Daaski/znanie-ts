import { Layout } from 'components/Layout/Layout';
import { Projects } from 'components/Projects';

const ProjectsPage = () => {
    return (
        <Layout title="Проекты общества Знания">
            <Projects />
        </Layout>
    );
};

export default ProjectsPage;
